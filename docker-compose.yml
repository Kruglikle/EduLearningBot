services:
  bot:
    build: .
    env_file:
      - .env
    volumes:
      - ./edulearning_bot.db:/app/edulearning_bot.db
      - ./bot/data/courses.json:/app/bot/data/courses.json:ro
      - ./exports:/app/exports
    ports:
      - "8080:8080"
    restart: unless-stopped
